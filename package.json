{
  "name": "url-title-scrubber",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec -u tdd test/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "~1.12.0"
  },
  "description": "Takes a string and formats it for a url."
}
